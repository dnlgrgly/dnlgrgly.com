import * as React from "react";
import Placeholder from "../components/Placeholder";

const NotFoundPage = () => (
  <Placeholder
    title="page not found (404)"
    subtitle="you followed a link that's broken. our construction workers are digging hard to help you out."
  />
);

export default NotFoundPage;
